import { Separator } from "@/components/ui/separator"

// A visible boundary between two parts of a page (owner, 2026-09-25: «не видно границ блоков»).
export function SectionSeparator() {
  return <Separator className="my-8" />
}
